fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
python-dotenv==1.0.1
httpx==0.27.0
numpy==1.26.4
scikit-learn==1.4.2
python-multipart==0.0.9
# Firestore — [async] extra installs grpc + async transport
google-cloud-firestore==2.16.1
# Auth — needed if using service account JSON directly
google-auth==2.29.0
# JWT — contributor sessions + lab partner API keys
PyJWT==2.8.0
